# Marcinek C++

Minimalny projekt C++ na CMake.

## Budowanie
```bash
cmake -S . -B build
cmake --build build
./build/my_project

## Jak odpalić (Linux/macOS – u Ciebie Debian też git)

W folderze projektu:
```bash
cmake -S . -B build
cmake --build build
./build/my_project